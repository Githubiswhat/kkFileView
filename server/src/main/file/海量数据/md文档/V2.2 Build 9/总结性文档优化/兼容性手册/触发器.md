# 触发器

当程序设计的某个条件成立的时候，触发器里面所定义的语句就会被自动的执行。本章节主要介绍Vastbase G100兼容Oracle触发器的相关内容，指导用户使用兼容Oracle的触发器功能。

<a href="Oracle兼容性/对象管理/触发器/兼容IF-Inserting-THENIF-updating-THENIF-deleting-THEN.html"><li>**兼容IF Inserting THEN,IF updating THEN,IF deleting THEN**</li></a>

<a href="Oracle兼容性/对象管理/触发器/兼容IF-UPDATINGcol1.html"><li>**兼容IF UPDATING('col1')**</li></a>

<a href="Oracle兼容性/对象管理/触发器/允许INSERT使用oid.colname获取值为空.html"><li>**允许INSERT使用oid.colname获取值为空**</li></a>